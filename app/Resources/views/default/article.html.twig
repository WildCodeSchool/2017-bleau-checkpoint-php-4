{% extends 'base.html.twig' %}
{% block body %}
	
	
<h2>Ajout Article</h2>
	<div class="add_page">
		<div class="container">
			{{ form_start(form) }}
			<div class="row col-sm-12">
				{{ form_row(form.name) }}
			</div>
			<div class="row col-sm-12">
				{{ form_row(form.amount) }}
			</div>
			<div class="row col-sm-12">
				{{ form_row(form.price ) }}
			</div>
			{{ form_end(form) }}
		</div>
	</div>
	

{% endblock %}